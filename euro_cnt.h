#include <cstdint>

extern uint8_t CalculateMaxPlayers();
extern uint8_t SetupControlMethods();
extern	void	SetupMouseLimits( int, int, int, int );
extern float WhichButton(float);
extern	void	GetConsoleInput();
