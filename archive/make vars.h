#define OFF					255#define ON 					1#define NO					0#define YES					1#define	English				0#define	French				1#define	German				2#define	American			3#define	Canadian			4#define	Norwegian			5#undef EURO96#define	DEBUGGING			ON		// ON or OFF#define MEM_DEBUG			OFF#define NET_DEBUG			OFF#define GAME_DEBUG			OFF#define	ROLLING_DEMO		OFF		// can be OFF, English, American#define BETA				0		// 11#define FINAL				14#define LANGUAGE			English#define	REAL_NAMES			YES		// can be YES or NO#define TWOKEY				YES		// Only One Person On the Keyboard#define USERAVEENGINE		YES#define COUNT				NO		// Show Polygon Counts#define ALLOW_8MEG			NO#if DEBUGGING==ON#include "Debugging.h"#endif#define MAX_NET_USERS		20#define	REAL_SPEED			15#define	TIMER_FRAC			4	// 10#define	TIMER_SPEED			(TIMER_FRAC*REAL_SPEED)#define NORMRES(X)			(128 + ((X)/8))#define GAMEDATA			"DATA"#define RESNAME				"\pFrontend Res"#define RESNAME2			"\pGame Res"#define MOVIERESFILE		"\pMovie Res"#define RESTYPE_DATA		'DATA'#define RESTYPE_FAP			'FAP '#define RESTYPE_ACTR		'ACTR'#define RESTYPE_TAC			'TAC '#define RESTYPE_MOVIE		'FLC '#define RESTYPE_MOVIEAUDIO	'snd '#define CONFIGTYPE			'Cnfg'#define PREFERENCENAME		"\pActua Soccer Pref"#define PREFERENCENAME2		"\pVRSoccer Pref"#define SAVEGAMETYPE		'Save'#define SAVEGAMECREATOR		'ACTU'				// So We Can Check It#define MinimumGameSize		3000 * 1024		/* Needs at least 64k of heap space. */enum	{gotNoEvent, gotOtherEvent, gotKeyEvent};enum 	{kTab = 9, kSpace = 20, kEsc = 27};#include "Resources.h"//#define DisplayWidth		640// Defines That Are Used Throghtout the code#define PI					3.14159265// Typedefstypedef unsigned char		BYTE;typedef unsigned short		WORD;typedef unsigned long		DWORD;// Inline code#define min(a,b) 			(((a)<(b))?(a):(b))#define max(X,Y)			(((X)>(Y))?(X):(Y))#if TWOKEY == YES#define	KEYBOARD			0#define KEYBOARD2			1#define JOYSTICK1			2#define JOYSTICK2			3#else#define	KEYBOARD			0#define JOYSTICK1			1#define JOYSTICK2			2#define JOYSTICK3			3#endif