#include "Mac Vars.h"#include "Actua.Soccer.h"#undef EXTERN#define EXTERN extern#include "Blitter.h"#include "Event.h"#include "FileUtils.h"#include "HideMenuBar.h"#include "Init.h"#include "Apple Menu.h"#include "PickAMonitor.h"#include "TitlePage.h"void restartProc(){	ExitToShell();}void main(void){Handle h;	MaxApplZone();							/* Expand the heap so code segments load at the top. */	Initialize(32, kMinHeap, kMinSpace);	/* Initialize the program. */	Gdh=PickAMonitor(8,TRUE,640,400,1);	BlitterInit();	while (!gQuitting)		GetAndProcessEvent();		BlitterEnd();}//void MacAttractMode()//{//}////void RefreshWTWindow()//{//}void BeginGame(void){	ToggleMenuBar();	FadeScreen( kDarken, Gdh );	DisplayFullscreenWindow(Gdh);	DisplayTitlePage(Gdh, UltraSoccerLogoResID);//	FadeScreen( kDarken, Gdh );	CloseFullScreen(Gdh);	ToggleMenuBar();}