#include "Mac Vars.h"#include "StringUtils.h"#undef EXTERN#define EXTERN externstatic short jstrlen(char *cptr);//char *jstrcpy(char *dest,char *src)//{//	while(*src)//	  *dest++=*src++;//	   //	return dest;	//}//char *jstrcat(char *dest,char *src)//{//	while(*dest)//	  dest++;//	while(*src)//	  *dest++=*src++;//	  //	return dest;//}void c2p(char *cptr){	char	len;	BlockMove(cptr, cptr + 1, len = jstrlen(cptr));	*cptr = len;}short	jstrlen(char *cptr){short	i=0;	while(*cptr)	  {	  cptr++;	  i++;	  }	  	return i;}